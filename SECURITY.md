# 보안 설계 메모 — v0.4.1

## 기본 원칙

- 서버 DB를 사용하지 않습니다.
- 공사정보와 업로드한 Excel 내용은 서버로 전송하지 않습니다.
- Excel 분석과 문서 생성은 브라우저에서 처리합니다.
- Analytics, 광고, 외부 오류수집 서비스를 사용하지 않습니다.
- 개인정보나 계약정보를 URL query string에 넣지 않습니다.
- 주민등록번호는 수집하지 않습니다.

## 브라우저 저장

공사·업체·하자검사·계산정보는 IndexedDB에 저장되며, 브라우저 데이터 삭제나 PC 교체 시 사라질 수 있으므로 전체 백업 기능을 사용해야 합니다.

## 지급정보

은행명·예금주·계좌번호는 공사 마스터와 분리된 `payouts` 저장소에서 관리합니다. 업체 목록과 일반 공사목록에는 계좌번호를 노출하지 않습니다.

전체 백업에는 지급정보가 포함될 수 있으므로 백업파일은 학교 내부 기준에 따라 안전하게 보관해야 합니다.

## 하자검사 기록

하자검사 기록은 공사 프로젝트 안에 누적 저장되며 외부 서버로 전송되지 않습니다.

## 외부 코드

필요 라이브러리는 프로젝트 안에 포함하고 외부 CDN 의존을 최소화합니다. Vercel 보안 헤더는 `vercel.json`에서 설정합니다.

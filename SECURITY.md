# 보안 설계 메모 — v0.3.0.2

## 저장되는 곳

공사, 업체, 학교 기본정보는 서버 DB가 아니라 해당 브라우저의 IndexedDB에 저장됩니다.

## 네트워크로 보내지 않는 것

- 사용자가 입력한 공사정보
- 업로드한 학교 공사 이력 Excel 내용
- 업로드한 K-에듀파인 자료관리목록 내용
- 전체 백업 JSON 내용

런타임 외부 CDN, Analytics, 광고, 오류수집 SDK, 외부 업무 API를 사용하지 않습니다.

## 의도적으로 저장하지 않는 정보

- 주민등록번호
- 업체 계좌번호
- 은행/예금주 등 지급계좌 정보

계좌정보가 필요한 공사서류를 추가할 때는 일반 공사 마스터와 분리된 보호영역을 별도로 설계합니다.

## 브라우저 로컬 저장의 한계

서버 전송이 없더라도 같은 OS/브라우저 사용자 프로필에 접근할 수 있는 사람은 저장된 데이터를 볼 수 있습니다. 공용 Windows 계정에서는 PC 자체의 접근통제가 필요합니다.

브라우저 데이터 삭제, PC 교체, 프로필 삭제 시 데이터가 사라질 수 있으므로 정기적으로 `전체 백업`을 사용합니다.

## 배포

Vercel용 `vercel.json`에는 CSP, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`가 포함되어 있습니다. 정적 HTML의 CSP meta도 함께 둡니다.

공사정보는 URL query string이나 hash에 포함하지 않습니다.

## 포함된 공사이력 템플릿

배포용 `assets/audit-template.xlsx`는 새 `학교 공사 이력 현황.xlsx`의 화면 서식은 유지하면서 개인 작성자명과 로컬 PC 경로 같은 문서 메타데이터가 포함되지 않도록 정리한 사본입니다. 샘플 행의 값은 실제 다운로드 시 공사 데이터로 대체됩니다.
